---
id: project.testing
summary: Уровни тестов, команды, отчёты, именование тестов по сценариям, среда e2e
read_when: Перед написанием тестов, реализацией и верификацией
updated: {{DATE}}
verification: needs-review
---

# Тестирование и проверки

## Уровни тестов

<!-- Таблица: уровень (unit / component / module / e2e) → когда применять → где лежат → чем запускаются. -->

## Команды

<!-- Узкий прогон одного файла, полный прогон, проверки типов и линтеров. Команды в обратных кавычках, например `pnpm test`.
     Обязательно опишите дымовой запуск: как поднять приложение или сервис локально (зависимости, порт) и какой URL или команда подтверждает, что оно работает. Верификатор запускает его, когда изменение добавляет модуль, маршрут или сборку. -->

## Проверка интерфейса в браузере

<!-- Если у продукта есть веб-интерфейс: базовый URL локального запуска (`browser.baseUrl` в .sbox/config.yaml), нужен ли вход и под каким профилем: человек входит один раз командой `sbox-browser login <url> --profile <имя>`, имя профиля укажите здесь и в `browser.profile`. Какие маршруты считать дымовыми для верификатора. Если интерфейса нет — «—». -->

## Отчёты

<!-- Куда пишутся отчёты тестов (jest --json, playwright json) и как их получить в CI. -->

## Именование тестов по сценариям

<!-- Правило: describe = название capability, вложенный describe = группа, it = утверждение. Пример. -->

## Среда e2e

<!-- Как запускаются e2e: локально, задачей CI, на стенде; что для этого нужно. Если e2e нет — «—». -->

## Не автоматизируется

<!-- Что проверяется только руками и почему. -->
