# User Profile Specification

## Purpose
Профиль пользователя: имя, аватар, настройки уведомлений.

## Requirements

### Requirement: Display name
The system SHALL show the user's display name in the header.

#### Scenario: Name is set
- **WHEN** the user has a display name
- **THEN** the header shows it
